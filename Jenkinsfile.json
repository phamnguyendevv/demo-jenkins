pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here, e.g., 'sh "make"' or 'sh "npm install"'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Add your test steps here, e.g., 'sh "make test"' or 'sh "npm test"'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add your deploy steps here, e.g., 'sh "make deploy"' or 'sh "npm run deploy"'
            }
        }
    }
}
